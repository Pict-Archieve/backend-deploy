// import { Request } from "express";

/**
 * Represents an HTTP request with a specific body type.
 * @typedef {import('express').Request} Request
 * @template T
 * @typedef {Request & { body: T }} TypeRequestBody
 */
